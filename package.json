{
  "name": "autoseo-service",
  "version": "1.0.0",
  "description": "Multi-tenant SEO automation service",
  "scripts": {
    "dev": "python app.py",
    "build": "echo 'Building...'",
    "start": "uvicorn app:app --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
